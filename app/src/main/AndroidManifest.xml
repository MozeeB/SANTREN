<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cikup.santren">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Santren">
        <activity android:name=".presentation.ui.absent.DetailAbsentActivity"></activity>
        <activity android:name=".presentation.ui.absent.ClassActivity" />
        <activity android:name=".presentation.ui.absent.AbsentActivity" />
        <activity android:name=".presentation.ui.report.ReportActivity" />
        <activity android:name=".presentation.ui.information.DetailInfoActivity" />
        <activity android:name=".presentation.ui.event.EventActivity" />
        <activity android:name=".presentation.ui.information.InformationActivity" />
        <activity android:name=".presentation.ui.splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.register.RegisterActivity" />
        <activity android:name=".presentation.ui.login.LoginActivity" />
        <activity android:name=".presentation.ui.dashboard.MainActivity" />
    </application>

</manifest>